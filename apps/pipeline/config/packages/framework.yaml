framework:
    secret: '%env(APP_SECRET)%'
    http_method_override: false
    handle_all_throwables: true
    php_errors:
        log: true
    # Registers the HttpClientInterface autowiring alias used by the Keepa client.
    http_client: ~

when@test:
    framework:
        test: true
